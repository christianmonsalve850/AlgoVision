import { LearningObjectives } from '@/features/lessons/components/learning-objectives'
import { Callout } from '@/features/lessons/components/callout'
import { PatternCard } from '@/features/lessons/components/pattern-card'
import { Checkpoint } from '@/features/lessons/components/checkpoint'

<div className="my-6 rounded-xl border border-neutral-800 bg-neutral-900/60 p-5 shadow-sm">
  <p className="text-sm font-semibold uppercase tracking-[0.2em] text-emerald-400">Welcome</p>
  <p className="mt-2 text-lg leading-relaxed text-neutral-200">Two Pointers is one of the most common interview techniques you'll encounter.</p>
  <p className="mt-3 text-sm leading-relaxed text-neutral-400">Instead of checking every possible combination, we use two indices that move intelligently through the data.</p>
</div>
<LearningObjectives 
    objectives = {[
        'Recognize Two Pointer problems',
        'Understand why the technique works',
        'Implement the pattern effectively',
        'Debug pointer movement',
        'Avoid common edge cases and mistakes',
    ]}
    title = {"What You'll Learn"}
/>

---

<Callout type="success">
    Mastering Two Pointers will immediately help with dozens of LeetCode problems.
</Callout>

---

<PatternCard
    title="Where You'll See It"
    items={[
        "Sorted arrays",
        "Pairs",
        "Triplets",
        "Palindromes",
        "Searching from both ends"
    ]}
/>

---

<Checkpoint>

Before continuing, ask yourself:

"Could two positions solve this problem faster than checking every pair?"

</Checkpoint>